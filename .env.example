# Optional: override defaults via environment variables
# FORCE_CPU=1          # set to use CPU (for local testing)
# DTYPE=bfloat16       # float16 (default) | bfloat16 | float32
# HF_TOKEN=hf_xxx      # Hugging Face token (if gated models)
